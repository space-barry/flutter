// lib/hadith_page.dart
import 'package:flutter/material.dart';

class HadithPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hadith Page'),
      ),
      body: Center(
        child: Text('Welcome to the hadith Page!'),
      ),
    );
  }
}
